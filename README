Metacritic Score Scraper v0.1 - Written by Assil Ksiksi
-------------------------------------------------------

Accepted command line arguments: -csv, -text, -out (default), or -help

-csv: reads input from csv (game,console) and writes to a new csv
-text: reads input from file (game:console) and writes to a new file
-out: prompts user to enter data (game:console) and outputs to terminal
-help: prints this info on screen

Make sure that module.py is in the same directory as main before running, because most of the script's logic is in module.py.

Note: to run this, you'll need httplib2. Get it from here:

http://code.google.com/p/httplib2/